name: build-api

on:
    push:
        branches: [main]
        paths:
            - "api/**"

jobs:
    build:
        uses: ./.github/workflows/docker-build.yaml
        with:
            service: api
        secrets: inherit