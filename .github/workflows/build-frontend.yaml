name: build-api

on:
    push:
        branches: [main]
        paths:
            - "frontend/**"

jobs:
    build:
        uses: ./.github/workflows/docker-build.yaml
        with:
            service: frontend
        secrets: inherit