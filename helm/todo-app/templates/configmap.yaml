apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  namespace: {{ .Values.namespace }}
data:
  REDIS_URL: "redis://redis:{{ .Values.redis.port }}"
  RABBITMQ_URL: "amqp://guest:guest@rabbitmq:{{ .Values.rabbitmq.port }}/"